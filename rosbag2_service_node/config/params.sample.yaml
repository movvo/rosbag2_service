# ===========================
#  Namespace (/** es todo)
# ===========================
/**:
  # ===================
  #     NODO
  # ===================
  ros__parameters:
    # ----- PARAMS -----
    autorun: true
    topics: [] #Write the topics to store in the rosbag here separated 
               #by commasa e.g. ["/topic1", "/topic2"] 